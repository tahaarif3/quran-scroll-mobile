# Design handoff note

Authoritative tokens and copy are taken from the handoff README (palette **1c** Sand + Gold/Olive) provided in chat.

If you later add `design_handoff_iqralock_onboarding/` to the repo:

1. Diff `IqraLockKit/DesignSystem/Tokens.swift` + `Typography.swift` against the README token table (README wins).
2. Side-by-side compare each screen to `screenshots/*.png` at 2×.
3. Open `IqraLock.dc.html` only to resolve layout questions the README doesn’t answer — never port the bezel/status bar.
